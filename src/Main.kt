import parkingSpace.*
import java.io.*


public class main{
    var menuType=0
    var continueFlow:Boolean=true
    var userInput:Int = readLine().toInt()
    val newBuilding= Building()


    fun main(args:String):Int{

    while (continueFlow)do{

        val menu= Building()
        println("true")}
        println("bye")
}
    fun getmenu(menuType:Int):String{
        return ""

    }

    fun isNameTaken(name:String):Boolean{
        newBuilding.levels[]
        if(it.name==name){
            return true
        } else{return false}
    }


       // revisa si el array que devuleve no es nulo
    // if (newBuilding.filterColor(color)!=null){


        //se puede agreagar


    fun isFileEligible(path:String):Boolean{
        return false
    }

    fun isIdTaken(id:Int):Boolean{

    }
}